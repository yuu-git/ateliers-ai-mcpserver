# Phase 2 実装の全体像

## 📐 アーキテクチャ概要

```
┌─────────────────────────────────────────────────────────────┐
│                      Claude Desktop                         │
│                    (MCPクライアント)                         │
└───────────────────────┬─────────────────────────────────────┘
                        │ stdio
                        │
┌───────────────────────▼─────────────────────────────────────┐
│              Ateliers.Training.McpServer                    │
│                     (MCPサーバー)                           │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Program.cs (エントリポイント)           │   │
│  │  - 設定読み込み (appsettings.json)                  │   │
│  │  - DIコンテナ設定                                   │   │
│  │  - MCPサーバー初期化                                │   │
│  └────────────┬────────────────────────────────────────┘   │
│               │                                             │
│  ┌────────────▼─────────────────────────────────────────┐  │
│  │              Services Layer                          │  │
│  │                                                      │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │         GitHubService                        │   │  │
│  │  │  - GitHub API接続                            │   │  │
│  │  │  - 匿名/PAT認証切り替え                      │   │  │
│  │  │  - メモリキャッシュ (5分)                    │   │  │
│  │  │  - ファイル取得/一覧取得                     │   │  │
│  │  │  - ローカルファイル対応（将来用）             │   │  │
│  │  └────────────┬─────────────────────────────────┘   │  │
│  └───────────────┼──────────────────────────────────────┘  │
│                  │                                          │
│  ┌───────────────▼──────────────────────────────────────┐  │
│  │              Tools Layer                             │  │
│  │                                                      │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │   AteliersGuidelineTools                     │   │  │
│  │  │   - ReadGuideline                            │   │  │
│  │  │   - ListGuidelines                           │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                                                      │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │   AteliersDevTools                           │   │  │
│  │  │   - ReadArticle                              │   │  │
│  │  │   - ListArticles                             │   │  │
│  │  │   - SearchArticles                           │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────┬───────────────────────────────────────────────┘
              │
              │ GitHub API (Octokit.NET)
              │
┌─────────────▼───────────────────────────────────────────────┐
│                      GitHub                                 │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  yuu-git/ateliers-ai-assistants                       │  │
│  │  - README.md, ガイドライン.md, etc.                   │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  yuu-git/ateliers-dev                                 │  │
│  │  - docs/*.md, docs/*.mdx (技術記事)                   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 データフロー

### 例1: ガイドライン読み取り

```
1. Claude Desktop
   ↓ "README.mdを読んで"
   
2. AteliersGuidelineTools.ReadGuideline("README.md")
   ↓
   
3. GitHubService.GetFileContentAsync("AteliersAiAssistants", "README.md")
   ↓
   3-1. キャッシュ確認
        - キャッシュヒット → 即座に返却
        - キャッシュミス → GitHub APIへ
   ↓
   3-2. GitHub API経由でファイル取得
        - Owner: yuu-git
        - Repo: ateliers-ai-assistants
        - Path: README.md
   ↓
   3-3. メモリキャッシュに保存（5分）
   ↓
   
4. ファイル内容を返却
   ↓
   
5. Claude Desktopに表示
```

### 例2: 記事検索

```
1. Claude Desktop
   ↓ "C#に関する記事を検索して"
   
2. AteliersDevTools.SearchArticles("C#", "docs")
   ↓
   
3. GitHubService.ListFilesAsync("AteliersDev", "docs", ".md")
   ↓
   3-1. キャッシュ確認
        - キャッシュヒット → ファイルリスト取得
        - キャッシュミス → GitHub APIで再帰的に取得
   ↓
   
4. 各ファイルの内容をチェック
   ↓
   4-1. ファイル名に"C#"が含まれる？
   4-2. ファイル内容に"C#"が含まれる？
   ↓
   
5. マッチした記事のリストを返却
   ↓
   
6. Claude Desktopに表示
```

---

## 🧩 コンポーネント詳細

### 1. Configuration Layer

**AppSettings.cs**
```csharp
// 型安全な設定管理
public class AppSettings
{
    public GitHubSettings GitHub { get; set; }
    public Dictionary<string, RepositorySettings> Repositories { get; set; }
}
```

**役割**:
- appsettings.jsonの構造を型定義
- DIコンテナに登録して全体で共有
- 将来の拡張に対応可能な柔軟な構造

### 2. Services Layer

**GitHubService.cs**
```csharp
public class GitHubService
{
    // GitHub API接続
    private readonly GitHubClient _client;
    
    // メモリキャッシュ
    private readonly IMemoryCache _cache;
    
    // 設定
    private readonly AppSettings _settings;
}
```

**役割**:
- GitHub APIとの通信を一元管理
- キャッシング戦略の実装
- ローカル/リモート切り替えロジック
- エラーハンドリング

**キャッシュ戦略**:
```csharp
// キャッシュキー設計
"github:{owner}/{repo}:{branch}:{path}"

// 例
"github:yuu-git/ateliers-ai-assistants:main:README.md"
```

### 3. Tools Layer

**AteliersGuidelineTools.cs**
```csharp
[McpServerToolType]
public class AteliersGuidelineTools
{
    // GitHubServiceをDIで注入
    public AteliersGuidelineTools(GitHubService gitHubService)
    {
        _gitHubService = gitHubService;
    }
}
```

**役割**:
- MCPツールとしてのインターフェース提供
- GitHubServiceを使用したビジネスロジック
- エラーハンドリングとユーザーへの返答

**AteliersDevTools.cs**

追加機能:
- Frontmatter除去
- キーワード検索
- マッチ行抽出

---

## 🎯 設計の重要ポイント

### 1. 責務の分離

```
┌────────────────────────────────────────────────┐
│  Tools Layer (AteliersGuidelineTools)         │
│  - ユーザーインターフェース                    │
│  - エラーメッセージ生成                        │
│  - ビジネスロジック                            │
└────────────┬───────────────────────────────────┘
             │ depends on
┌────────────▼───────────────────────────────────┐
│  Services Layer (GitHubService)                │
│  - データ取得ロジック                          │
│  - キャッシング                                │
│  - GitHub/ローカル切り替え                     │
└────────────┬───────────────────────────────────┘
             │ uses
┌────────────▼───────────────────────────────────┐
│  Configuration Layer (AppSettings)             │
│  - 設定の型定義                                │
│  - 設定値の保持                                │
└────────────────────────────────────────────────┘
```

### 2. 拡張性の確保

**将来の拡張ポイント**:

```csharp
// ✅ 認証モード追加
"AuthenticationMode": "OAuth" // 将来対応

// ✅ 新しいデータソース
"Source": "AzureBlob" // 将来対応

// ✅ 新しいリポジトリ追加
"Repositories": {
  "NewRepository": { ... }
}

// ✅ キャッシュ戦略変更
"CacheStrategy": "Redis" // 将来対応
```

### 3. テスタビリティ

**DIによるテスト容易性**:

```csharp
// モックを注入してテスト可能
var mockService = new Mock<GitHubService>();
var tools = new AteliersGuidelineTools(mockService.Object);
```

---

## 📊 パフォーマンス特性

### キャッシュなしの場合

```
1回目: GitHub API呼び出し (200-500ms)
2回目: GitHub API呼び出し (200-500ms)
3回目: GitHub API呼び出し (200-500ms)
...
60回目: レート制限エラー
```

### キャッシュありの場合（Phase 2実装）

```
1回目: GitHub API呼び出し (200-500ms) → キャッシュ保存
2回目: キャッシュヒット (1-5ms) ⚡
3回目: キャッシュヒット (1-5ms) ⚡
...
5分後: キャッシュ期限切れ
次回: GitHub API呼び出し (200-500ms) → キャッシュ更新
```

**効果**:
- レスポンス速度: 約100倍高速化
- レート制限回避: 5分以内の再アクセスはカウントされない

---

## 🔐 セキュリティ考慮事項

### 現在の実装（匿名アクセス）

**安全性**:
- ✅ パブリックリポジトリのみアクセス
- ✅ 認証情報不要
- ✅ サンプルとして公開可能

**制限**:
- ❌ プライベートリポジトリ不可
- ❌ レート制限が厳しい（60/時間）

### 将来の拡張（PAT使用）

**appsettings.json**:
```json
{
  "GitHub": {
    "PersonalAccessToken": "ghp_xxxx"
  }
}
```

⚠️ **注意**: PATをハードコードせず、環境変数から取得推奨

```csharp
// 推奨実装
var token = Environment.GetEnvironmentVariable("GITHUB_PAT");
```

---

## 🚦 エラーハンドリング戦略

### レイヤーごとの責務

```
Tools Layer:
  - ユーザーフレンドリーなメッセージ生成
  - エラーをキャッチして文字列で返す
  
Services Layer:
  - 例外をスロー（FileNotFoundException等）
  - リトライロジック（将来実装）
  
GitHub API:
  - RateLimitExceededException
  - NotFoundException
  - NetworkException
```

### 実装例

```csharp
// Tools Layer
try
{
    var content = await _gitHubService.GetFileContentAsync(...);
    return content;
}
catch (FileNotFoundException ex)
{
    return $"File not found: {ex.Message}";
}
catch (Exception ex)
{
    return $"Error reading file: {ex.Message}";
}
```

---

## 📈 今後の改善ポイント

### Phase 3以降の実装予定

1. **リトライロジック**
```csharp
// Pollyライブラリ使用
var retryPolicy = Policy
    .Handle<Exception>()
    .WaitAndRetryAsync(3, retryAttempt => 
        TimeSpan.FromSeconds(Math.Pow(2, retryAttempt)));
```

2. **構造化ログ**
```csharp
_logger.LogInformation(
    "GitHub file fetched: {Owner}/{Repo}/{Path}",
    owner, repo, path
);
```

3. **メトリクス収集**
```csharp
// キャッシュヒット率
// API呼び出し回数
// レスポンス時間
```

---

**作成日**: 2025-11-23  
**Phase**: 2 - GitHub統合完了  
**次のアクション**: ファイル配置 → ビルド → 動作確認 → GitHubコミット
